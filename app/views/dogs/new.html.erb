<h1>上の句</h1>

<!--<%= render 'form', dog: @dog %>-->

<%= form_with(model: @dog) do |form| %>

  <div class="field">
    <%= form.text_field :kaminoku %>
  </div>

  <div class="actions">
    <%= form.submit "登録"%>
  </div>
<% end %>

<%= link_to 'Back', dogs_path %>
