<%= form_with(model: pig) do |form| %>
  <% if pig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pig.errors.count, "error") %> prohibited this pig from being saved:</h2>

      <ul>
        <% pig.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :nakanoku_pig %>
  </div>

  <div class="actions">
    <%= form.submit "登録"%>
  </div>
<% end %>
